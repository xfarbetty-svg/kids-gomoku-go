# 🎙️ 影子跟讀重大重構任務書 (V5) — 正統「A 念一句、B 跟讀一句」逐句互動模式

## 一、 問題診斷與使用者痛點
目前影子跟讀為「整篇連續播放，句間只停 600ms」，使用者完全沒有開口跟讀該句的空間，錄音也是整篇一次錄完，導致無法針對單一句子模仿語調、無法得到單句即時精準評分。這完全偏離了影子跟讀（Shadowing / Echo Method）在教學現場「A 講完一句 ➔ 暫停/留白 ➔ B 跟讀該句 ➔ AB 比對糾錯 ➔ 前進下一句」的標準訓練邏輯。

---

## 二、 核心重構目標：逐句沉浸式跟讀訓練（Focus Sentence Echo Flow）

### 1. 介面層重構（焦點單句大卡片 + 單句操作列）
在 `#shadowScriptArea` 上方或取代混亂的整篇長文，新增**「焦點單句卡片（Focus Sentence Card）」**：
- **頂部進度**：顯示「進度：第 X / Y 句」與動態百分比進度條。
- **單句主體**：
  - 第一行：日文原文（帶 `<ruby><rt>` 雙層漢字假名標音）。
  - 第二行：紫色等寬標準羅馬拼音（Hepburn Romaji）或英文音標。
  - 第三行：繁體中文對照翻譯。
  - 第四行：💡 發音訣竅與斷句停頓提示（`pauseHint`）。
- **單句核心控制列（三顆醒目按鈕）**：
  1. `[ 🔊 聽這句示範 ]`：只朗讀當前這一句（採用 OYASUMI 慢速自然 Voice）。
  2. `[ 🎙️ 輪到我跟讀 ]`：針對當前單句啟動錄音與語音辨識；朗讀完畢後立刻計算該單句的 Levenshtein 相似度，即時彈出星級評分（⭐⭐⭐）與回饋！
  3. `[ 🎧 AB 對照 ]`：單句完成錄音後，可一鍵切換「聽原音」與「聽自己的單句錄音」。
- **導航列**：
  - `[ ⬅️ 上一句 ]`、`[ 下一句 ➡️ ]`、以及【自動前進開關（打完勾自動進下一句）】。
- **下方篇章導覽清單**：保留整篇所有句子的迷你清單，點擊任一句可直接跳轉至該句作為焦點。

### 2. 兩種訓練模式開關
在影子跟讀控制頂部提供切換：
- **【逐句精練模式 (Echo/Repeat)】（預設）**：A 念完當前句，自動停住，等待使用者按「輪到我跟讀」或自動倒數引導使用者跟讀，完成後再進入下一句。
- **【自動輪播跟讀模式 (Auto Flow)】**：點擊「連續跟讀」時，A 念完第 1 句 ➔ 畫面高亮顯示「🎙️ 請跟讀！」並預留該句子時間的 1.5 倍（約 3~5 秒）供使用者開口模仿 ➔ 倒數結束後再播放第 2 句！絕對不可連珠砲狂播！

### 3. Service Worker 更新
快取版本推進至 `kids-games-v25`。
